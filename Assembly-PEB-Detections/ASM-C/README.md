#Gcc&Masm syntax for inline assembly

    __asm__
    (
      ;code
      ;code
    );
      
#Visual Studio

    __asm
    {
      ;code
      ;code
    }
  
VS much eaiser but I can't use 'cos not enough ram to use it lol.
